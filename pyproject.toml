[project]
name = "replit-workspace-cap-matrix"
version = "0.0.1"
description = "Scaffold: Workspace Cap Matrix (independent Replit lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
